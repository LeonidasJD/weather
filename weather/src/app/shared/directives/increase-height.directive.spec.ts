import { IncreaseHeightDirective } from './increase-height.directive';

describe('IncreaseHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new IncreaseHeightDirective();
    expect(directive).toBeTruthy();
  });
});
